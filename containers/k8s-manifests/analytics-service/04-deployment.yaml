apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: analytics-service
  labels:
    app: analytics-service
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
        version: "1.0"
    spec:
      containers:
      - name: analytics-service
        image: 589138972429.dkr.ecr.us-east-1.amazonaws.com/a/analytics-service:v1
        imagePullPolicy: IfNotPresent
        ports:
        - name: http
          containerPort: 8005
          protocol: TCP
        
        # Variáveis de ambiente
        env:
        - name: AWS_SQS_URL
          valueFrom:
            secretKeyRef:
              name: analytics-service-secrets
              key: AWS_SQS_URL
        - name: AWS_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: analytics-service-secrets
              key: AWS_ACCESS_KEY_ID
        - name: AWS_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: analytics-service-secrets
              key: AWS_SECRET_ACCESS_KEY
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: analytics-service-config
              key: AWS_REGION
        - name: AWS_DYNAMODB_TABLE
          valueFrom:
            configMapKeyRef:
              name: analytics-service-config
              key: AWS_DYNAMODB_TABLE
        - name: APP_NAME
          valueFrom:
            configMapKeyRef:
              name: analytics-service-config
              key: APP_NAME
        - name: LOG_LEVEL
          valueFrom:
            configMapKeyRef:
              name: analytics-service-config
              key: LOG_LEVEL
        - name: PORT
          valueFrom:
            configMapKeyRef:
              name: analytics-service-config
              key: PORT
        
        # Resource requests e limits
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 200m
            memory: 256Mi
        
        # Readiness Probe
        # readinessProbe:
        #   httpGet:
        #     path: /health
        #     port: 8005
        #     scheme: HTTP
        #   initialDelaySeconds: 15
        #   periodSeconds: 10
        #   timeoutSeconds: 5
        #   successThreshold: 1
        #   failureThreshold: 3
        
        # # Liveness Probe
        # livenessProbe:
        #   httpGet:
        #     path: /health
        #     port: 8005
        #     scheme: HTTP
        #   initialDelaySeconds: 30
        #   periodSeconds: 15
        #   timeoutSeconds: 5
        #   successThreshold: 1
        #   failureThreshold: 3
        
        # # Startup Probe
        # startupProbe:
        #   httpGet:
        #     path: /health
        #     port: 8005
        #     scheme: HTTP
        #   initialDelaySeconds: 5
        #   periodSeconds: 5
        #   timeoutSeconds: 3
        #   failureThreshold: 30
      
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                - key: app
                  operator: In
                  values:
                  - analytics-service
              topologyKey: kubernetes.io/hostname
